# 프로젝트 문서 구조 (Documentation Structure)

이 프로젝트는 **Vibe Coding**과 **MVI + Clean Architecture**를 지원합니다.

## 📂 폴더 구조 (Folder Structure)

### `00-meta` (메타 정보)
- 프로젝트 전반의 규칙, 목표, 원칙.
- `01-product-spec.md`: 제품 사양서 (Product Spec v1.0).

### `10-domain` (도메인 지식)
- 비즈니스 핵심 개념, 용어 정의, 알고리즘 설명.
- `05-analysis-logic.md`: 기질 분석 엔진 로직.
- `04-survey-design-bars.md`: BARS 기반 설문 설계.
- `prompts/`: LLM 분석 리포트 생성 프롬프트 관리.

### `15-data-model` (데이터 모델)
- DB 스키마(SQL), ERD.

### `20-usecase` (비즈니스 로직 명세)
- **Core Business Rules**. UI와 무관한 순수 로직 (`docs/_templates/02-usecase-spec.md` 참조).
- Screen 문서에서 이를 참조(Reference)하여 사용합니다.
- 예: `03-gardening.md` (양육 코칭/정원 로직).

### `30-screen` (화면 명세)
- **UI & Interaction Spec** (`docs/_templates/01-screen-spec.md` 참조).
- MVI 패턴의 **Intent**에서 `20-usecase`를 호출하는 형태로 기술합니다.

### `40-api` (API 명세)
- 백엔드 연동 규격.

### `50-ui` (디자인 시스템)
- `01-design-system.md`: 컴포넌트 및 스타일 가이드.
- 컬러 시스템, 아이콘 등.

---

## 🛠 개발 단위 (Dev Units)
1.  **Use Case**: 비즈니스 로직 단위 (`20-usecase`).
2.  **Screen**: UI 화면 단위 (`30-screen`).
3.  **Feature**: 배포 단위 (Use Case + Screen의 집합).
